import { STORAGE_KEYS } from '../utils/storage/constants';

// ... rest of the file remains the same, but use STORAGE_KEYS.ANALYTICS instead of 'game_analytics'
const ANALYTICS_KEY = STORAGE_KEYS.ANALYTICS;