import { STORAGE_KEYS } from '../utils/storage/constants';

// ... rest of the file remains the same, but use STORAGE_KEYS.ACHIEVEMENTS instead of 'game_achievements'
const ACHIEVEMENTS_KEY = STORAGE_KEYS.ACHIEVEMENTS;